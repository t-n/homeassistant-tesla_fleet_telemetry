name: Integration CI

on:
  push:
  pull_request:

jobs:
  hassfest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run hassfest
        run: |
          pip install hassfest
          hassfest -c custom_components/tesla_fleet_stream
